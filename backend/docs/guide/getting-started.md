# 使用指南

本文档介绍工作流系统的使用方法。

## 节点配置

每个节点都有以下通用配置：

- **名称**: 节点的显示名称
- **描述**: 节点的详细说明（可选）

## 数据传递

节点之间通过 `{{input.xxx}}` 语法传递数据。

### 示例

```
{{input.userId}}
{{input.data.result}}
```

## 错误处理

如果某个节点执行失败，工作流会：
1. 记录错误信息
2. 可选择继续执行或停止
